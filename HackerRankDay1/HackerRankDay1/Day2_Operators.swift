//
//  Day2_Operators.swift
//  HackerRankDay1
//
//  Created by Eric Davenport on 11/11/20.
//

import Foundation

//class Test {
//// Complete the solve function below.
//
//  guard let meal_cost = Double(((readLine()?.trimmingCharacters(in: .whitespacesAndNewlines))!))
//  else { fatalError("Bad input") }
//
//  guard let tip_percent = Int((readLine()?.trimmingCharacters(in: .whitespacesAndNewlines))!)
//  else { fatalError("Bad input") }
//
//  guard let tax_percent = Int((readLine()?.trimmingCharacters(in: .whitespacesAndNewlines))!) else { fatalError("Bad input") }
//
//func solve(meal_cost: Double, tip_percent: Int, tax_percent: Int) -> Void {
//  let tip = (Double(tip_percent / 100) * meal_cost)
//  let tax = (Double(tax_percent / 100) * meal_cost)
//  let mealTotal = meal_cost + tip + tax
//
//  print(mealTotal)
//
//}
//
//
//solve(meal_cost: meal_cost, tip_percent: tip_percent, tax_percent: tax_percent)
//
//}
//let meal_cost = Double(readLine()!) ?? 0.0
//let tip_percent = Int(readLine()!) ?? 0
//let tax_percent = Int(readLine()!) ?? 0
//
//let tip = (Double(tip_percent / 100) * meal_cost)
//let tax = (Double(tax_percent / 100) * meal_cost)
//let mealTotal = meal_cost + tip + tax

//print(mealTotal)
//func printTotal() {
//  print(mealTotal)
//}
//printTotal()

// MARK: correct running code on main.swift - issues in this page

// Complete the solve function below.
// copy and pasted work from HackerRank
/*
func solve(meal_cost: Double, tip_percent: Int, tax_percent: Int) -> Void {
  let tip = (Double(tip_percent) / 100.00) * meal_cost
  let tax = (Double(tax_percent) / 100.00) * meal_cost
  let mealTotal = round(meal_cost + tip + tax)
  
  print(Int(mealTotal))

}

guard let meal_cost = Double((readLine()?.trimmingCharacters(in: .whitespacesAndNewlines))!)
else { fatalError("Bad input") }

guard let tip_percent = Int((readLine()?.trimmingCharacters(in: .whitespacesAndNewlines))!)
else { fatalError("Bad input") }

guard let tax_percent = Int((readLine()?.trimmingCharacters(in: .whitespacesAndNewlines))!)
else { fatalError("Bad input") }

solve(meal_cost: meal_cost, tip_percent: tip_percent, tax_percent: tax_percent)

*/
